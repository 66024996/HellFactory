<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>โปรไฟล์ผู้ใช้ - <%= user.username %></title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: url('/image/Profile.png') no-repeat center center/cover;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .profile-container {
            background: rgba(0, 0, 0, 0.7);
            color: white;
            padding: 20px;
            border-radius: 15px;
            text-align: center;
            width: 400px;
        }
        .profile-pic {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            object-fit: cover;
            background: gray;
            border: 3px solid #FF9B81;
        }
        .upload-form {
            margin-top: 10px;
        }
        .info-box {
            background: #FF9B81;
            padding: 15px;
            border-radius: 10px;
            margin: 10px 0;
            font-size: 18px;
        }
        .edit-form {
            display: none;
        }
        .logout-button, .edit-button, .save-button {
            padding: 10px;
            margin-top: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .logout-button { background: red; color: white; }
        .edit-button, .save-button { background: #F2CC8F; color: black; }
    </style>
</head>
<body>
    <div class="profile-container">
        <h2>โปรไฟล์ของ <%= user.username %></h2>
        
        <!-- รูปโปรไฟล์ -->
        <img src="<%= user.profile_picture ? user.profile_picture : '/uploads/default.png' %>" 
             class="profile-pic" id="profilePic">
        
        <!-- ฟอร์มอัปโหลดรูป -->
        <form action="/upload-profile" method="POST" enctype="multipart/form-data">
            <input type="file" name="profilePic" accept="image/*">
            <input type="hidden" name="userId" value="<%= user.id %>"> <!-- ✅ ต้องมีค่า -->
            <button type="submit" class="edit-button">อัปโหลดรูป</button>
        </form>
        
        
        

        <div class="info-box"><b>ชื่อ:</b> <%= user.first_name %> <%= user.last_name %></div>
        <div class="info-box"><b>อีเมล:</b> <%= user.email %></div>
        <div class="info-box"><b>เบอร์โทร:</b> <%= user.phone || "ยังไม่ได้ระบุ" %></div>

        <button class="edit-button" onclick="toggleEdit()">แก้ไขข้อมูล</button>

        <!-- ฟอร์มแก้ไขข้อมูล -->
        <form action="/update-profile/<%= user.id %>" method="POST" class="edit-form" id="editForm">
            <input type="text" name="first_name" value="<%= user.first_name %>">
            <input type="text" name="last_name" value="<%= user.last_name %>">
            <input type="text" name="phone" value="<%= user.phone || '' %>">
            <input type="text" name="email" value="<%= user.email || '' %>">
            <button type="submit" class="save-button">บันทึก</button>
        </form>

        <button class="logout-button" onclick="logout()">ออกจากระบบ</button>
    </div>

    <script>
        function toggleEdit() {
            const form = document.getElementById('editForm');
            form.style.display = form.style.display === 'block' ? 'none' : 'block';
        }

        function logout() {
            window.location.href = "/";
        }
    </script>
</body>
</html>
