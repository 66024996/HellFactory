<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Statistics Chart</title>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
</head>
<body>
    <canvas id="statsChart"></canvas> <!-- 🔹 ตำแหน่งของกราฟ -->
    
    <script>
        fetch("http://localhost:3000/stat")  // ⬅️ เรียก API
            .then(response => response.json())
            .then(data => {
                const months = data.statistics.map(stat => stat.month);   // ✅ ดึงชื่อเดือน
                const guests = data.statistics.map(stat => stat.guests);  // ✅ ดึงจำนวนผู้ใช้บริการ
                const revenue = data.statistics.map(stat => stat.revenue); // ✅ ดึงรายได้

                const ctx = document.getElementById("statsChart").getContext("2d");
                new Chart(ctx, {
                    type: "bar", // ✅ ใช้ bar chart (แท่งกราฟ)
                    data: {
                        labels: months,  // ✅ ใช้ชื่อเดือนเป็นแกน X
                        datasets: [
                            {
                                label: "Guests", 
                                data: guests,
                                backgroundColor: "rgba(54, 162, 235, 0.6)" // 🔹 สีน้ำเงิน
                            },
                            {
                                label: "Revenue",
                                data: revenue,
                                backgroundColor: "rgba(255, 99, 132, 0.6)" // 🔹 สีแดง
                            }
                        ]
                    },
                    options: {
                        responsive: true,
                        scales: {
                            y: { beginAtZero: true } // ✅ แกน Y เริ่มจาก 0
                        }
                    }
                });
            })
            .catch(error => console.error("❌ Error fetching data:", error));
    </script>
</body>
</html>
