<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Phayao Place</title>
    <style>
        body, html {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            height: 100%;
        }
        .background {
            background: url('image/lobby.jpg') no-repeat center center/cover;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
        }
        .overlay {
            width: 100%;
            height: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            position: absolute;
        }
        .login-box {
            background: rgba(0, 0, 0, 0.85);
            padding: 40px 20px;
            border-radius: 10px;
            text-align: center;
            color: white;
            max-width: 400px;
            width: 100%;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3);
        }
        .logo {
            width: 250px;
            height: 200px;
            background: no-repeat center/cover;
        }
        .tagline {
            font-size: 1.1em;
            color: #fff;
            margin-bottom: 20px;
        }
        .input-field {
            width: 95%;
            padding: 10px;
            margin: 10px 0;
            border: none;
            border-radius: 5px;
            font-size: 1em;
            background: rgba(133, 122, 122, 0.777);
            color: #ffffff;
        }
        .input-field::placeholder {
            color: rgba(255, 255, 255, 0.7);
        }
        .login-button {
            width: 100%;
            padding: 10px;
            background: #e67e22;
            border: none;
            border-radius: 5px;
            color: white;
            font-size: 1em;
            cursor: pointer;
            margin-top: 20px;
        }
        .login-button:hover {
            background: #d35400;
        }
        .footer-links {
            margin-top: 15px;
            font-size: 0.9em;
        }
        .footer-links a {
            color: #f39c12;
            text-decoration: none;
        }
        .footer-links a:hover {
            text-decoration: underline;
        }
        .popup {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.8);
            display: flex;
            justify-content: center;
            align-items: center;
            visibility: hidden;
            opacity: 0;
            transition: visibility 0s, opacity 0.5s;
        }
        .popup-content {
            background: white;
            padding: 20px;
            border-radius: 10px;
            text-align: center;
            max-width: 400px;
            width: 80%;
            color: black;
        }
        .popup-content p {
            margin-bottom: 20px;
        }
        .popup-content button {
            padding: 10px 20px;
            margin: 5px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }
        .popup-content button.confirm {
            background: #e67e22;
            color: white;
        }
        .popup-content button.cancel {
            background: gray;
            color: white;
        }
        .popup.visible {
            visibility: visible;
            opacity: 1;
        }
    </style>
</head>
<body>
    <div class="background">
        <div class="overlay">
            <div class="login-box">
                <img src="image/โลโก้สีส้ม.png" alt="Phayao Place Logo" class="logo">
                <p class="tagline">ค้นหาบัญชีของคุณ</p>
                <p>กรุณากรอก Username Account ของท่าน เพื่อค้นหาบัญชีของท่าน</p>
                <form id="searchForm">
                    <input type="text" placeholder="ค้นหา Username Account" class="input-field" required>
                    <button type="submit" class="login-button">ค้นหา</button>
                    <button type="button" class="login-button" style="background: gray;">ยกเลิก</button>
                </form>
            </div>
        </div>
    </div>

    <div class="popup" id="popup">
        <div class="popup-content">
            <p>คุณต้องการที่จะ Reset รหัสผ่านใช่หรือไม่?</p>
            <button class="confirm">ดำเนินการ</button>
            <button class="cancel">ยกเลิก</button>
        </div>
    </div>

    <script>
        const searchForm = document.getElementById('searchForm');
        const popup = document.getElementById('popup');
        const confirmButton = document.querySelector('.popup-content .confirm');
        const cancelButton = document.querySelector('.popup-content .cancel');

        searchForm.addEventListener('submit', function(event) {
            event.preventDefault();
            popup.classList.add('visible');
        });

        cancelButton.addEventListener('click', function() {
            popup.classList.remove('visible');
        });

        confirmButton.addEventListener('click', function() {
            alert('Password reset process initiated.');
            popup.classList.remove('visible');
        });
    </script>
</body>
</html>
